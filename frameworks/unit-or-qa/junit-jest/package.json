{
  "name": "benchzoo-junit-jest-sample",
  "version": "0.0.0",
  "private": true,
  "description": "Canonical sample benchmark expressed as Jest unit tests; fixture source for the junit_jest parser.",
  "scripts": {
    "test": "jest --reporters=default --reporters=jest-junit"
  },
  "devDependencies": {
    "jest": "29.7.0",
    "jest-junit": "16.0.0"
  },
  "jest-junit": {
    "outputDirectory": "./",
    "outputName": "output.xml"
  }
}
