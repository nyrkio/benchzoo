name: tinybench

on:
  push:
    branches: [main]
    paths:
      - 'frameworks/language/tinybench/**'
      - '.github/workflows/tinybench.yml'
  pull_request:
    paths:
      - 'frameworks/language/tinybench/**'
      - '.github/workflows/tinybench.yml'
  workflow_dispatch:
  schedule:
    # Daily at 12:00 UTC for the test 4 change-detection showcase.
    # Avoid 0 0 * * * — month boundary ambiguity.
    - cron: '53 20 * * 3'

permissions:
  contents: read

concurrency:
  group: tinybench-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frameworks/language/tinybench
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frameworks/language/tinybench/package.json

      - name: Run sample benchmark
        run: bash run.sh

      - name: Upload output as artifact
        uses: actions/upload-artifact@v4
        with:
          name: tinybench-output
          path: frameworks/language/tinybench/output.json
          if-no-files-found: error
