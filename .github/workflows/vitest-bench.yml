name: vitest-bench

on:
  push:
    branches: [main]
    paths:
      - 'frameworks/language/vitest-bench/**'
      - '.github/workflows/vitest-bench.yml'
  pull_request:
    paths:
      - 'frameworks/language/vitest-bench/**'
      - '.github/workflows/vitest-bench.yml'
  workflow_dispatch:
  schedule:
    # Daily at 12:00 UTC for the test 4 change-detection showcase.
    # Avoid 0 0 * * * — month boundary ambiguity.
    - cron: '0 12 * * *'

permissions:
  contents: read

concurrency:
  group: vitest-bench-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frameworks/language/vitest-bench
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frameworks/language/vitest-bench/package.json

      - name: Run sample benchmark
        run: bash run.sh

      - name: Upload output as artifact
        uses: actions/upload-artifact@v4
        with:
          name: vitest-bench-output
          path: frameworks/language/vitest-bench/output.json
          if-no-files-found: error
